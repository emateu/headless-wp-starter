query GetSiteSettings {
  siteSettings {
    siteConfig {
      siteLogo {
        node {
          ...MediaItemFields
        }
      }
      socialLinks {
        platform
        url
      }
      copyrightText
    }
  }
}
