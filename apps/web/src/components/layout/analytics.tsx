/**
 * Analytics placeholder — replace the contents with your analytics provider
 * (e.g. GA4, Plausible, Fathom) when ready. No code changes needed elsewhere.
 */
export function Analytics() {
  return null;
}
